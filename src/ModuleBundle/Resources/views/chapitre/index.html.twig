{% extends 'backend/indexe.html.twig' %}
{% block content %}
    <div class="box">
        <div class="box-header">
            <h3 class="box-title">Liste des chapitre</h3>
        </div>
        <!-- /.box-header -->
        <div class="box-body">
            <table id="example1" class="table table-bordered table-striped">
                <thead>
                <tr>
                    <th>ID chapitre</th>
                    <th>Nom du chapitre</th>
                    <th>Description</th>
                    <th>Dans la matière</th>
                    <th>Télécharger le chapitre</th>
                    <th>Action</th>
                </tr>
                </thead>
                <tbody>
                {% for chapitre in chapitres %}
                    <tr>
                        <td><a href="{{ path('chapitre_show', { 'id': chapitre.id }) }}">{{ chapitre.id }}</a></td>
                        <td>{{ chapitre.titre }}</td>
                        <td>{{ chapitre.description }}</td>
                        <td></td>
                        <td></td>
                        <td>
                            <ul>
                                <li>
                                    <a href="{{ path('chapitre_show', { 'id': chapitre.id }) }}">show</a>
                                </li>
                                <li>
                                    <a href="{{ path('chapitre_edit', { 'id': chapitre.id }) }}">edit</a>
                                </li>
                            </ul>
                        </td>
                    </tr>
                {% endfor %}
                </tbody>

                </table>
        </div>
        <!-- /.box-body -->
    </div>
    <!-- /.box -->
    </div>
    <!-- /.col -->
    </div>
    <!-- /.row -->
    </section>
    <!-- /.content -->
    </div>



{% endblock %}
