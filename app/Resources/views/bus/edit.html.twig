{% extends 'backend/index.html.twig' %}

{% block content %}
    <h1 style="text-align: center">Modifier bus</h1>
<div class="box-body">
    <table id="example2" class="table table-bordered table-hover">
        <thead>
        <tr>

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
            <div class="">
        <input type="submit" value="Edit" /> <a type="button" class="btn btn-default pull-right" href="{{ path('bus_index') }}">Back to the list</a>
                {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
                {{ form_end(delete_form) }}
            </div>
    {{ form_end(edit_form) }}



        </tr>
        </thead>
    </table>
</div>
{% endblock %}
