{% extends 'backend/index.html.twig' %}

{% block content %}
    <h1 style="text-align: center">Ligne edit</h1>


    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <input type="submit" value="Edit" />
    {{ form_end(edit_form) }}




        <a type="button" class="btn btn-default pull-right" href="{{ path('ligne_index') }}">Back to the list</a>

            {{ form_start(delete_form) }}
                <input style="color: red" type="submit" value="Delete">
            {{ form_end(delete_form) }}



{% endblock %}
